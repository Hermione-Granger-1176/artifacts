# Verification

## Prompt Caching, Demystified

### Automated

- `tests/js/apps/prompt-caching/modules.test.js` unit-tests every export of
  `math.js` and asserts the integrity of `data.js` (vocab ordering, attention rows
  summing to ~1, embedding catalogue completeness, section/summary shape).
- Coverage of the tested modules: `math.js` 100% lines / ~96% branches / 100%
  functions, `data.js` 100%. This is comfortably above the repo gate (95/85/95).
- `make check-local` runs lint (ESLint/stylelint), the JS tests, coverage,
  security audit, artifact validation, and generated-drift checks.

### Calculations spot-checked by hand

- **Savings calculator:** 2000 tokens × 500 req/day × 30 days = 30M tokens/month;
  without caching = 30 × $3 = **$90/mo**; with 80% cached at 1/10 price =
  0.2·30·$3 + 0.8·30·$0.30 = 18 + 7.2 = **$25.20/mo**; savings **$64.80/mo**.
  The model intentionally omits the one-time 25% cache-write surcharge and cache
  expiry (see `docs/decisions.md`); it reflects the steady-state read price, so it
  slightly overstates savings at very low request volumes.
- **Softmax:** verified the produced weights sum to 1 and the largest score wins.
- **Cosine similarity:** identical vectors → 1.0, orthogonal → 0, opposite → < 0.

### Manual / in-browser

- Confirm no CSP violations in the console (no inline style/script, no CDN fonts).
- Confirm light and dark both render correctly, including the two canvases
  (dimension explorer + similarity plot redraw on theme toggle).
- Exercise each demo: inference Generate/Reset, tokenizer text/ID toggle, embedding
  word/pair selection + Swap, attention step navigation + matrix cell clicks + mask
  toggle + grid hover + softmax sliders, KV-cache Play/Reset + compare toggle,
  cache-hit Send/Clear + TTL countdown, and the savings sliders.
- Thumbnails are generated by CI (`make check` / deploy), not required locally.
